import React from 'react';

import Slide from '../../components/Slide';

const SmartDump = ({ isActive }) => (
  <Slide isActive={isActive} center>
    <div className="slide-head">Smart/Dump components</div>
  </Slide>
);

export default SmartDump;
