import React from 'react';

import Slide from '../../components/Slide/Slide';

const Thanks = ({ isActive }) => (
  <Slide isActive={isActive} center>
    <div className="slide-head">Thanks!</div>
  </Slide>
);

export default Thanks;
